#!/bin/bash

alias atualizar="sudo apt-get update && sudo apt-get upgrade && sudo snap refresh"

alias desligar="sudo shutdown -h now"

alias reiniciar="sudo shutdown -r now"

alias apagar="rm -rf"

alias deletar="rm -rf"

alias remover="rm -rf"
